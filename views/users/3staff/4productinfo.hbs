<div class="main product-info">
    <div id="product-info-container">
        <h1>Product Information</h1>
        <div>
            {{#each product.images}}
            <img src="{{this}}" alt="image" class="design-image">
            {{/each}}
        </div>
        <div>Desription
            <ul>
                <li id="product-name">Name: {{product.name}}</li>
                <li>Designer: {{product.designer.firstName}} {{product.designer.lastName}}</li>
                <li>Editors:
                    <ul>
                        {{#each product.editors}}
                        <li>{{this.firstName}} {{this.lastName}}</li>
                        {{/each}}
                    </ul>
                </li>
                <li>Category: {{product.category}}</li>
                <li>Color: {{product.color}}</li>
                <li>Material: {{product.material}}</li>
                <li>Collection: {{product.serie.season}} {{product.serie.year}}</li>
                <li>Status: {{product.status}}</li>
                <li>Internal notes: {{product.internalNotes}}</li>
                <li>id: <span id="product-id">{{product._id}}</span></li>
                {{#if isDesigner}}
                <li><a href="/head/edit-product/{{product._id}}"><img src="/images/editicon.png" alt="Edit"
                            class="icon"></a></li>
                {{/if}}
            </ul>
        </div>
    </div>
    <div id="comments-container">
        <h2>Comments</h2>
        {{> flashmessages}}
        <div>
            <input type="text" name='comment-input' placeholder="type..." id="comment-input">
            <button id='send-btn'>Send</button>
        </div>
        {{#each comments}}
        <div>
            <div>{{this.content}}</div>
            <div>{{this.author.firstName}} {{this.author.lastName}} {{date this.date}}</div>
        </div>
        {{/each}}
    </div>
</div>